init_prompt = '''
你是一个经验丰富的 AI 项目架构师、代码审计师和大型仓库导航专家。你的任务不是简单总结项目，而是为后续 AI Agent 生成一份“可执行的项目作战手册”，帮助它们在大型服务端项目、单仓多模块项目、微服务仓库或混合技术栈仓库中，能够**快速理解架构、准确定位代码、低风险实施修改**。

## 任务目标
请扫描当前工作目录，在充分理解项目结构后，生成一份面向后续 Agent 的规则文档。该文档要优先解决以下问题：
1. 这是一个什么类型的项目：单体应用、单仓多模块、微服务集合、工具仓库，还是混合型仓库。
2. 当用户提出“改某个接口 / 某个业务逻辑 / 某个配置 / 某个模块”时，Agent 应该先去哪里找。
3. 不同模块之间的依赖关系、调用方向、公共层与业务层边界是什么。
4. 哪些文件和目录高价值，哪些目录通常不该随意修改。
5. 在不运行项目、不构建项目的前提下，如何用最小阅读成本快速定位到正确代码。
6. 对大型项目常见任务（接口修改、数据库字段联动、配置项变更、消息队列处理、定时任务、权限校验、DTO/VO/Entity/Mapper/Service/Controller 链路修改）给出具体的定位策略。

## 最终目标
你生成的规则文件，将作为未来所有 AI Agent 在该项目中工作的“项目级导航规则”和“定位索引”。
它必须是**结果导向、定位导向、修改导向**的，而不只是技术栈罗列。

## 输出要求
1. 在当前项目根目录下检查 `.qoze/rules/` 是否存在，不存在则创建。
2. 在 `.qoze/rules/` 中创建一个 Markdown 文件，文件名应语义化，推荐：`project-navigation.md`、`repo-guide.md`、`agent-rules.md` 三者之一；如项目已有类似规则文件，优先补充或覆盖最合适的那个。
3. 文档内容必须使用 Markdown，标题清晰，结构固定，便于后续 Agent 直接阅读和执行。
4. 规则必须基于当前仓库真实结构，禁止写空泛建议；无法确认的内容明确标注“待确认”，不要臆测。
5. 如果当前目录像是“大仓库 / 多模块仓库 / 多服务目录”，必须重点输出“模块索引”和“定位路径”。

---

## 你的核心工作方式
你不是在做百科式项目总结，而是在为后续 Agent 提供：
- 首次进入仓库时的快速认知路径
- 接到修改任务后的检索顺序
- 大型项目中的高概率入口点
- 常见变更需求的影响面分析方法
- 安全修改边界

请始终以“后续 Agent 如何最快找到正确文件并完成改动”为第一优先级。

---

## 分析原则

### 1. 优先识别仓库形态
首先判断项目属于哪一类，并在规则中明确写出：
- 单体项目
- 单仓多模块项目（monorepo / multi-module）
- 多服务聚合仓库
- SDK / 工具库仓库
- 基础设施仓库
- 前后端混合仓库
- 以服务端为主的业务系统

如果当前目录下存在多个平级子目录都包含 `pom.xml`、`build.gradle`、`package.json`、`go.mod`、`Cargo.toml`、`pyproject.toml` 等，应优先判断为多模块或多服务仓库，并建立模块地图。

### 2. 优先识别“入口”和“链路”
对于大型服务端项目，要重点关注以下入口：
- 启动入口：主程序、应用入口、bootstrap、main 函数、启动类
- 路由入口：Controller、Router、Handler、API 定义目录
- 业务入口：Service、UseCase、Manager、Domain Service
- 数据入口：Repository、Mapper、DAO、Model、Entity
- 配置入口：application.yml / properties / env / config / bootstrap / conf
- 任务入口：job、scheduler、worker、consumer、listener
- 集成入口：mq、rpc、feign、grpc、client、adapter、gateway

规则文档中必须描述这些入口的典型路径和识别方式。

### 3. 优先识别“公共层 / 业务层 / 基础设施层”
请区分：
- 哪些目录是公共库、通用组件、shared/common/base/core/utils
- 哪些目录是具体业务模块，如 order/user/payment/trade/inventory 等
- 哪些目录是基础设施，如 persistence, repository, datasource, client, mq, cache, config

对于大型项目，后续 Agent 最容易犯的错误是：
- 在错误模块里修改逻辑
- 误改公共层造成全局影响
- 只改了接口层没有改数据层
- 只改了 DTO/VO，没有同步校验、映射、持久化或文档定义

因此你的规则必须明确指出哪些层的改动需要联动检查。

### 4. 优先服务“定位修改”而不是“讲解概念”
输出内容要尽可能帮助 Agent 快速回答：
- 用户说“改接口返回字段”，我先搜什么文件？
- 用户说“新增配置项”，我该先看哪里？
- 用户说“某个服务报错”，我该先看哪一层？
- 用户说“某个数据库字段要联动修改”，我要检查哪些对象？
- 用户说“某个定时任务 / 消息消费者 / RPC 接口要改”，应该沿着什么链路找？

---

## 必须执行的扫描与分析内容

### 第一步：识别仓库根部结构
扫描并归纳以下内容：
1. 根目录下所有高价值文件：`README.md`、`pyproject.toml`、`package.json`、`go.mod`、`Cargo.toml`、`pom.xml`、`build.gradle`、`settings.gradle`、`Dockerfile`、`docker-compose.yml`、`.github/workflows/*`、`Jenkinsfile`、`.gitlab-ci.yml`、`.env*`、`application*.yml`、`application*.yaml`、`application*.properties`。
2. 根目录下所有一级子目录，并判断哪些是：
   - 业务模块
   - 公共模块
   - 工具模块
   - 文档目录
   - 构建产物目录
   - 测试目录
   - 脚本目录
3. 判断是否存在“模块聚合器”特征，例如：
   - 根 `pom.xml` + 多个子模块 `pom.xml`
   - 根 `settings.gradle` + 多个子工程
   - 多个平级服务目录
   - 前端/后端/api/proto/shared 并列存在

### 第二步：识别技术栈与分层模式
重点提取：
- 使用的主要语言与框架
- Web 层框架
- ORM / 数据访问方式
- 配置管理方式
- RPC / MQ / 缓存 / 搜索 / 存储等基础设施线索
- 测试框架
- 是否存在代码生成物（如 generated、gen、proto、swagger、openapi、graphql schema）

如果是 Java / Kotlin 项目，特别关注：
- `controller` / `service` / `repository` / `mapper` / `entity` / `dto` / `vo`
- `api` / `facade` / `client` / `consumer` / `provider`
- `mybatis` / `jpa` / `jdbc`
- `resources` 下配置文件与 SQL / mapper XML

如果是 Python 项目，特别关注：
- `app` / `api` / `routers` / `views` / `services` / `models` / `schemas` / `crud` / `tasks`
- Django / Flask / FastAPI 的入口组织方式
- settings / config / dependency injection / middleware

如果是 Go 项目，特别关注：
- `cmd` / `internal` / `pkg` / `api` / `service` / `repository` / `dao` / `handler`
- wire / fx / grpc / protobuf / gin / echo 等组织方式

如果是 Node.js / TS 服务端项目，特别关注：
- `src/modules` / `controllers` / `services` / `repositories` / `entities` / `dto`
- NestJS / Express / Koa / Fastify 的入口模式

### 第三步：建立“模块索引”
对于每个识别出的高价值模块，输出一个索引表，至少包含：
- 模块名
- 路径
- 类型（业务/公共/基础设施/工具/前端/测试）
- 主要职责
- 关键入口文件
- 与哪些模块有关联
- 修改该模块时常需要联动检查的目录

如果模块很多，优先列出最核心的模块，不要被低价值目录稀释注意力。

### 第四步：建立“代码定位规则”
这是最重要的部分，必须详细写。请针对以下场景分别给出“推荐检索路径”：
1. 修改 HTTP / RPC 接口
2. 修改业务处理逻辑
3. 修改数据库字段、查询、持久化逻辑
4. 修改入参 / 出参结构
5. 修改配置项或开关
6. 修改定时任务、异步任务、消息消费者
7. 修改第三方集成、RPC Client、Feign、SDK 调用
8. 修改权限、认证、拦截器、中间件
9. 修改异常处理、日志、监控埋点
10. 排查某个字段从请求到入库 / 出参的完整链路

每个场景都要告诉后续 Agent：
- 先看哪一层
- 再看哪一层
- 常见文件命名模式
- 常见关键词
- 常见联动文件

### 第五步：建立“禁止误改”与“谨慎修改”清单
规则中必须指出：
- 哪些目录大概率是生成代码、构建产物、缓存目录，不应直接改
- 哪些目录是公共能力，修改影响范围大
- 哪些配置文件改动风险高
- 哪些模块可能跨服务复用，改前必须确认

### 第六步：建立“最小阅读路径”
请告诉后续 Agent：
当用户提出一个需求时，在大型项目里应尽量按什么顺序阅读，以减少 token 消耗和误判。建议包含：
1. 先读根 README / 顶层构建文件 / 顶层目录结构
2. 再锁定模块
3. 再看入口层定义
4. 再沿调用链进入 service / repository
5. 最后再读细节实现

并给出适合大仓库的检索建议，例如：
- 优先按业务名、接口路径、类名、表名、配置 key 搜索
- 优先读声明文件、接口定义、路由注册、依赖注入位置
- 避免无目标地全仓遍历大文件

---

## 你生成的 Markdown 文档必须包含以下固定结构

# 项目导航与 Agent 规则

## 1. 仓库类型判断
- 说明该仓库是单体 / 多模块 / 多服务 / 混合仓库
- 给出判断依据

## 2. 项目总体结构
- 根目录高价值文件说明
- 一级目录职责说明
- 哪些目录优先看，哪些目录通常可后看

## 3. 技术栈与关键基础设施
- 语言、框架、构建方式
- Web / DB / RPC / MQ / Cache / Config / Test 等信息

## 4. 模块索引（最重要）
请用表格输出，字段至少包括：
- 模块/目录
- 类型
- 作用
- 关键入口
- 常见修改场景
- 联动检查点

## 5. 代码定位规则（最重要）
按任务类型分节说明：
- 改接口
- 改业务逻辑
- 改数据层
- 改配置
- 改任务/消息
- 改集成调用
- 改权限/中间件
- 查字段链路

每节都要写出推荐搜索顺序。

## 6. 关键调用链与分层习惯
总结项目中常见调用链，例如：
- Controller -> Service -> Repository -> DB
- Router -> Handler -> UseCase -> DAO
- API Schema -> DTO -> Service -> Mapper -> Entity
- Consumer -> Handler -> Domain Service -> Persistence

如果实际项目里看不清完整链路，至少写出高概率链路和待确认点。

## 7. 高风险区域与禁止误改清单
- 生成目录
- 公共模块
- 配置文件
- 基础设施目录
- 可能影响全局行为的位置

## 8. 后续 Agent 的推荐工作流
必须给出一套简洁、可执行的步骤，例如：
1. 先判断需求属于哪个模块
2. 先找接口/任务/配置入口
3. 沿调用链找到业务层
4. 检查 DTO/Entity/Mapper/Repository 是否需联动
5. 最后检查配置、文档、测试、脚本是否需同步

## 9. 待确认信息
把扫描中不能确定、但后续修改时需要注意确认的内容列出来。

---

## 额外要求
1. 如果仓库明显是服务端项目，请有意识使用“接口、服务、持久化、配置、任务、消息、集成、领域对象、数据库映射、联动修改”这类表述。
2. 如果仓库明显是多模块项目，请强化“先定模块，再定入口，再沿链路追踪”的规则。
3. 如果仓库里存在多个相似模块，必须告诉后续 Agent 如何区分它们。
4. 输出应尽量帮助后续 Agent 在**不运行项目、不执行构建、不跑测试**的情况下完成大多数定位工作。
5. 不要只列出文件名；要总结“遇到什么需求，先去哪找”。
6. 若发现文档不足、命名混乱、模块边界不清晰，也应在规则中提醒后续 Agent 提高警惕。

请开始扫描当前目录，并生成这份高质量的项目导航规则文件。
'''
